// Deployment settings for the public site. Leave empty when the site and the
// API are served from the same domain (the API is then reached at "/api").
// If the API lives on its own origin, set it here, e.g.:
//   window.GRILL_OUT_CONFIG = { apiBaseUrl: "https://api.example.com/api" };
window.GRILL_OUT_CONFIG = {};
